🎯 Частые проблемы и решения
Проблема: "Address already in use"
bash
# Найдите процесс, использующий порт 80
sudo lsof -i :80
# Убейте процесс или настройте Nginx на другой порт

Проблема: "Permission denied"
bash
# Проверьте права доступа к папке с сайтом
sudo chown -R www-data:www-data /var/www/my-site
sudo chmod -R 755 /var/www/my-site

Проблема: "Failed to bind"
bash
# Проверьте, не запущен ли другой веб-сервер
sudo systemctl stop apache2  # если используется Apache
sudo systemctl start nginx