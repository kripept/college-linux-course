
### **📄 lessons/03-web-server/labs/practice.md**
```markdown
# Практические задания - Урок 3

## 🎯 Задание 1: Создание персонального сайта

Создайте персональный сайт-портфолио со следующей структурой:
/var/www/portfolio/
├── index.html # Главная страница
├── about.html # О себе
├── projects.html # Учебные проекты
├── css/
│ └── style.css # Стили
└── images/
└── avatar.jpg # Ваше фото

text

**Требования:**
- На главной странице должна быть информация о вас
- Страница "О себе" с вашими интересами в IT
- Страница с перечнем выполненных проектов в колледже
- Единый CSS файл для стилизации всех страниц

## 🎯 Задание 2: Настройка нескольких сайтов

Создайте два виртуальных хоста:

1. **Сайт-портфолио** (как в задании 1)
2. **Технический блог** с статьями о Linux

**Конфигурация:**
- Портфолио доступен по `http://ваш-ip:8080`
- Блог доступен по `http://ваш-ip:8081`
- Разные папки для каждого сайта
- Разные файлы логов

## 🎯 Задание 3: Безопасность и оптимизация

Настройте базовую безопасность для Nginx:

```bash
# 1. Создайте отдельного пользователя для Nginx (если нужно)
sudo useradd -r -s /bin/false nginx-user

# 2. Настройте правильные права для файлов
sudo chown -R nginx-user:nginx-user /var/www/portfolio

# 3. Настройте брандмауэр
sudo ufw allow 'Nginx Full'
sudo ufw enable

# 4. Создайте скрипт для мониторинга Nginx
#!/bin/bash
# monitor-nginx.sh
if ! systemctl is-active --quiet nginx; then
    echo "Nginx is down! Restarting..."
    systemctl start nginx
fi

🎯 Задание 4: Автоматизация развертывания
Создайте скрипт deploy-site.sh, который:

Останавливает Nginx

Создает бэкап текущего сайта

Копирует новые файлы из папки updates/

Устанавливает правильные права

Запускает Nginx и проверяет статус

📝 Контрольные вопросы
Объясните разницу между: sites-available и sites-enabled

Что делает команда: sudo nginx -t и почему она важна?

Как перенаправить трафик с HTTP на HTTPS?

Что такое виртуальные хосты и зачем они нужны?

Как настроить Nginx для обслуживания статических файлов?

🏆 Дополнительное задание (для продвинутых)
Настройте Nginx как обратный прокси для приложения:

nginx
server {
    listen 80;
    server_name _;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /static/ {
        root /var/www/college-site;
        expires 30d;
    }
}
