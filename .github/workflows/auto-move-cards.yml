name: 🚀 Автоматическое перемещение карточек

on:
  pull_request:
    types: [opened, ready_for_review, closed]

jobs:
  move-project-card:
    runs-on: ubuntu-latest
    steps:
      - name: 📊 Перемещаем карточку студента
        uses: actions/github-script@v6
        with:
          script: |
            // Этот скрипт будет автоматически перемещать карточки
            // когда студенты создают Pull Requests
            console.log('Автоматизация карточек для PR:', context.payload.pull_request.number);
            
            // Здесь будет код для автоматического перемещения
            // Нужно настроить после тестирования ручного процесса
