
### **📄 lessons/04-monitoring/labs/practice.md**
```markdown
# Практические задания - Урок 4

## 🎯 Задание 1: Комплексный мониторинг системы

Создайте скрипт `full-monitor.sh`, который собирает полную информацию о системе:

**Требования к скрипту:**
- Отчет в формате HTML с цветовым выделением
- Проверка критических параметров (память < 10%, диск > 90%)
- Отправка отчета на email (если настроен)
- Логирование результатов в файл

## 🎯 Задание 2: Система алертинга

Настройте систему оповещений о проблемах:

```bash
# 1. Создайте скрипт для проверки критических параметров
#!/bin/bash
# alert-system.sh

# Проверка памяти
MEM_FREE=$(free | grep Mem | awk '{printf "%.0f", $4/$2 * 100}')
if [ $MEM_FREE -lt 10 ]; then
    echo "ALERT: Мало свободной памяти! ($MEM_FREE% свободно)"
fi

# Проверка диска
DISK_USAGE=$(df / | grep / | awk '{print $5}' | sed 's/%//g')
if [ $DISK_USAGE -gt 90 ]; then
    echo "ALERT: Мало места на диске! ($DISK_USAGE% занято)"
fi

# Добавьте проверки для CPU, сети, служб

🎯 Задание 3: Автоматизация обслуживания

Создайте набор cron задач для автоматического обслуживания:

Ежедневные задачи:

Очистка временных файлов

Ротация логов

Проверка обновлений безопасности

Еженедельные задачи:

Полный бэкап системы

Очистка кэша пакетов

Генерация отчетов

🏆 Дополнительное задание (для продвинутых)
Настройте мониторинг с помощью Prometheus Node Exporter:

# Установка Node Exporter
wget https://github.com/prometheus/node_exporter/releases/download/v1.3.1/node_exporter-1.3.1.linux-amd64.tar.gz
tar xvfz node_exporter-1.3.1.linux-amd64.tar.gz
cd node_exporter-1.3.1.linux-amd64

# Запуск Node Exporter
./node_exporter &

# Проверка в браузере
# http://ваш-сервер:9100/metrics