# Лабораторная работа 4: Мониторинг системы

## 📋 Задание
Освоить инструменты мониторинга системы, управление процессами и настройку автоматических задач.

## 🛠️ Инструкция

### Часть 1: Мониторинг системных ресурсов

#### Установка инструментов мониторинга
```bash
# Обновляем систему
sudo apt update

# Устанавливаем утилиты мониторинга
sudo apt install -y htop iotop nmon dstat sysstat tree

# Устанавливаем утилиты для сети
sudo apt install -y nethogs iftop

# 1. htop - интерактивный мониторинг процессов
htop

# 2. iotop - мониторинг дисковых операций
sudo iotop

# 3. nmon - комплексный мониторинг
nmon

# 4. vmstat - статистика виртуальной памяти
vmstat 1 10

# 5. iostat - статистика дисковых операций
iostat -x 1 5

# Информация о памяти
free -h
cat /proc/meminfo

# Информация о дисках
df -h
lsblk

# Информация о процессоре
lscpu
cat /proc/cpuinfo

# Информация о сети
ip addr show
ss -tulpn

# Поиск процессов по имени
pgrep nginx
ps aux | grep nginx

# Детальная информация о процессе
ps -p $(pgrep nginx) -o pid,ppid,cmd,%cpu,%mem,start

# Дерево процессов
pstree
pstree -p | grep nginx

# Запуск процесса с низким приоритетом
nice -n 10 ./my-script.sh

# Изменение приоритета запущенного процесса
renice -n 5 $(pgrep nginx)

# Просмотр приоритетов
ps -eo pid,pri,ni,comm | head -10

# Отправка сигналов процессу
sudo kill -TERM $(pgrep nginx)      # Graceful shutdown
sudo kill -HUP $(pgrep nginx)       # Перезагрузка конфигурации
sudo kill -KILL $(pgrep nginx)      # Принудительное завершение

# Проверка всех сигналов
kill -l

# Системные логи
sudo tail -f /var/log/syslog
sudo journalctl -f

# Логи авторизации
sudo tail -f /var/log/auth.log

# Логи Nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# Поиск ошибок в логах
sudo grep -i error /var/log/syslog
sudo journalctl -p err

# Логи за определенный период
sudo journalctl --since "2023-12-20 00:00:00" --until "2023-12-20 23:59:59"

# Логи конкретной службы
sudo journalctl -u nginx
sudo journalctl -u ssh

# Просмотр конфигурации logrotate
cat /etc/logrotate.conf
ls /etc/logrotate.d/

# Принудительный запуск ротации
sudo logrotate -vf /etc/logrotate.conf

# Редактирование cron задач для текущего пользователя
crontab -e

# Просмотр текущих задач
crontab -l

# Удаление всех задач
crontab -r

# Добавьте эти строки в crontab -e

# Ежедневное обновление системы в 3:00
0 3 * * * sudo apt update && sudo apt upgrade -y

# Еженедельная очистка временных файлов в воскресенье в 4:00
0 4 * * 0 sudo find /tmp -type f -atime +7 -delete

# Мониторинг диска каждый день в 6:00
0 6 * * * df -h > /home/admin/disk-usage.txt

# Перезагрузка сервера в первое воскресенье месяца в 2:00
0 2 1-7 * 0 [ $(date +\%u) -eq 7 ] && sudo reboot

# Системные задачи (требуют sudo)
sudo crontab -e

# Пример системной задачи - бэкап каждую ночь
0 2 * * * /usr/local/bin/backup-system.sh

# Создаем скрипт
nano /usr/local/bin/system-check.sh

#!/bin/bash
# Скрипт проверки системных ресурсов

echo "=== СИСТЕМНЫЙ МОНИТОРИНГ ==="
echo "Время: $(date)"
echo ""

# Загрузка системы
echo "💻 ЗАГРУЗКА СИСТЕМЫ:"
uptime
echo ""

# Память
echo "🧠 ПАМЯТЬ:"
free -h
echo ""

# Диски
echo "💾 ДИСКИ:"
df -h | grep -v tmpfs
echo ""

# Сеть
echo "🌐 СЕТЕВЫЕ СОЕДИНЕНИЯ:"
ss -tulpn | grep LISTEN
echo ""

# Процессы (топ-5 по памяти)
echo "🔥 ТОП-5 ПРОЦЕССОВ ПО ПАМЯТИ:"
ps aux --sort=-%mem | head -6

# Даем права на выполнение
sudo chmod +x /usr/local/bin/system-check.sh

# Тестируем скрипт
/usr/local/bin/system-check.sh

# Создаем скрипт мониторинга Nginx
nano /usr/local/bin/nginx-monitor.sh

#!/bin/bash
# Мониторинг состояния Nginx

LOG_FILE="/var/log/nginx-monitor.log"
NGINX_STATUS=$(systemctl is-active nginx)

echo "$(date): Проверка Nginx" >> $LOG_FILE

if [ "$NGINX_STATUS" != "active" ]; then
    echo "❌ Nginx не работает! Попытка перезапуска..." >> $LOG_FILE
    sudo systemctl start nginx
    
    # Проверяем успешность перезапуска
    sleep 5
    NEW_STATUS=$(systemctl is-active nginx)
    
    if [ "$NEW_STATUS" = "active" ]; then
        echo "✅ Nginx успешно перезапущен" >> $LOG_FILE
    else
        echo "💥 Не удалось запустить Nginx!" >> $LOG_FILE
        # Здесь можно добавить отправку уведомления
    fi
else
    echo "✅ Nginx работает нормально" >> $LOG_FILE
fi

# Делаем скрипт исполняемым
sudo chmod +x /usr/local/bin/nginx-monitor.sh

# Добавляем в cron для проверки каждые 5 минут
crontab -e
# Добавляем строку:
*/5 * * * * /usr/local/bin/nginx-monitor.sh

📊 Проверка работы
Тест 1: Мониторинг ресурсов
bash
# Проверяем что утилиты установлены
which htop iotop nmon

# Проверяем работу htop
htop --version
Тест 2: Управление процессами
bash
# Проверяем что можем управлять процессами
sudo systemctl status nginx
pgrep nginx
Тест 3: Cron задачи
bash
# Проверяем что задачи добавлены
crontab -l
Тест 4: Скрипты мониторинга
bash
# Проверяем работу скриптов
/usr/local/bin/system-check.sh
sudo /usr/local/bin/nginx-monitor.sh

🐛 Решение проблем
Проблема: "crontab: command not found"
Решение: Установите cron: sudo apt install cron

Проблема: Скрипт не выполняется из cron
Решение: Убедитесь что указаны полные пути к командам в скрипте

Проблема: Нет прав на запись логов
Решение: Используйте sudo или настройте права: sudo chmod 666 /var/log/nginx-monitor.log

